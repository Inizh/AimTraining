[project]
name = "aimtraining"
version = "0.1.0"
description = "This project uses SDL for rendering but still works faster"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pygame-ce>=2.5.5",
]
